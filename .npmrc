# Expo/Metro do not resolve pnpm's default isolated (symlinked) node_modules well.
# A hoisted linker gives a flat node_modules like npm/yarn, which the mobile app needs.
node-linker=hoisted

# Keep the lockfile honest across the workspace
shared-workspace-lockfile=true
strict-peer-dependencies=false
auto-install-peers=true
